@model IEnumerable<Zazzo.ChoresWizard2000.Models.FamilyMember>
@{
    ViewData["Title"] = "Family Members";
}

<header class="page-heading">
    <div>
        <h1>👨‍👩‍👧‍👦 Family Members</h1>
        <p>Manage who participates in the monthly Sorting Ceremony.</p>
    </div>
    <wa-button href="@Url.Action("Create")" variant="success" appearance="filled">
        <wa-icon slot="start" name="user-plus"></wa-icon>
        Add New Member
    </wa-button>
</header>

<div class="table-shell">
    <table class="data-table">
        <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Parent</th>
                <th scope="col">Teen</th>
                <th scope="col">Active</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td data-label="Name"><strong>@item.Name</strong></td>
                    <td data-label="Parent"><wa-icon name="@(item.IsParent ? "circle-check" : "circle-minus")" variant="@(item.IsParent ? "solid" : "regular")"></wa-icon></td>
                    <td data-label="Teen"><wa-icon name="@(item.IsTeen ? "circle-check" : "circle-minus")" variant="@(item.IsTeen ? "solid" : "regular")"></wa-icon></td>
                    <td data-label="Active">
                        <wa-badge variant="@(item.IsActive ? "success" : "neutral")" pill>
                            @(item.IsActive ? "Active" : "Inactive")
                        </wa-badge>
                    </td>
                    <td data-label="Actions">
                        <div class="table-actions">
                            <wa-button href="@Url.Action("Edit", new { id = item.Id })" size="small" variant="brand" appearance="outlined">Edit</wa-button>
                            <wa-button href="@Url.Action("Delete", new { id = item.Id })" size="small" variant="danger" appearance="outlined">Delete</wa-button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div class="page-actions">
    <wa-button href="@Url.Action("Index", "Home")" variant="neutral" appearance="outlined">
        <wa-icon slot="start" name="arrow-left"></wa-icon>
        Back to Home
    </wa-button>
</div>
